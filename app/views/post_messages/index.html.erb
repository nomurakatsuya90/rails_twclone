<h1>tweet一覧</h1>
<p><%= notice %></p>
<table>
  <tr>
    <th>内容</th>
  </tr>
<% @post_messages.each do |post_message| %>　　
  <tr>
    <td><%= post_message.content %></td>
    <td><%= link_to "tweetを編集する", edit_post_message_path(post_message.id), data: { confirm: '本当に編集していいですか？' } %></td>
    <td><%= link_to 'tweetを削除する', post_message_path(post_message.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくtweetを投稿する', new_post_message_path %>